# Frontend Execution-Flow Notes

Frontend-only. See [`dev-notes/README.md`](../README.md) for what this
file is, why it's separate from the backend log, and the conventions
below.

Append-only. For each feature/change: what components/hooks/state exist
now, what got added/removed, and how data actually flows end-to-end
(component -> hook/store -> API call -> response handling). Newest entry
at the bottom.

No entries yet.
